# Dependencies
/node_modules

# Next.js build output
/.next
/out

# Production build
/build

# Environment files (local only)
.env*.local
.env.local

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Railway (local link config only — railway.json stays in repo)
/.railway

# OS
.DS_Store
*.pem
Thumbs.db

# Debug logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# Firebase (no longer used)
/.firebase
firebase-debug.log
serviceAccountKey.json

# Vercel (no longer used)
/.vercel

# IDE
.vscode/
.idea/

# Capacitor — native projects are scaffolded per-developer (see CAPACITOR.md)
/ios
/android
