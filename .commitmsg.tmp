fix(toggle-gate+prep-summary): grey out canteen ON until menu+slots saved, add explicit Save Menu button, prep summary loads resilient

- app/api/canteen/prep-summary/route.ts: resilient select retries with base
  columns if availability_type/is_meal missing in prod (kills 'Failed to load
  prep summary' banner).
- app/vendor/dashboard/page.tsx:
  * Topbar canteen toggle now visually disabled (opacity 0.45, cursor not-allowed)
    and the input disabled when slotsConfigured && menuConfigured is false.
    Tooltip names the missing step.
  * Refreshes the configured flags on every sidebar tab change so the toggle
    updates without a reload.
  * Menu & Items: removed auto-mark of vendor_menu_configured. Added explicit
    'Save Menu' button. Any edit invalidates the saved flag, forcing re-save
    (matches PDF 'new or unchanged' rule).
  * Prep Summary empty state shows a slot-selector pill per the revised PDF mock.
- README updated.

142/142 tests pass; production build clean.
